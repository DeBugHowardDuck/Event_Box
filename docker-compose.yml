services:
  postgres:
    image: postgres:16
    environment:
      POSTGRES_DB: event_box
      POSTGRES_USER: event_box
      POSTGRES_PASSWORD: event_box
    ports:
      - "5433:5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data

  redis:
    image: redis:7
    ports:
      - "6379:6379"
    volumes:
      - redis_data:/data

volumes:
  postgres_data:
  redis_data: